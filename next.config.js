/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false, // DO NOT TURN THIS ON. IT CAUSES _app.tsx to run twice for some god forsaken reason.
};

module.exports = nextConfig;
